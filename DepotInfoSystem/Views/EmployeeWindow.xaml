<Window x:Class="DepotInfoSystem.Views.EmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DepotInfoSystem.ViewModels"
        mc:Ignorable="d"
        Title="Employees"
        Width="420" Height="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FFF9FAFB"
        FontFamily="Segoe UI"
        d:DataContext="{d:DesignInstance Type=vm:EmployeeViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#B22222"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#E8E8E8"/>
        <SolidColorBrush x:Key="AccentColor" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="ButtonForeground" Color="White"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonForeground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#8B1A1A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#660000"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#DDDDDD"/>
                                <Setter Property="Foreground" Value="#999999"/>
                                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Width="100" Height="120"
                BorderBrush="Gray" BorderThickness="1"
                Background="#FFDBDBDB" Margin="0,0,0,10"
                HorizontalAlignment="Left" VerticalAlignment="Top">
            <Image Source="{Binding Employee.PhotoImage}" Stretch="Uniform"/>
        </Border>
        <Button Content="Photo" Width="80" Height="35"
                HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="110,0,0,10"
                Command="{Binding SelectPhotoCommand}"/>

        <Label Grid.Row="1" Content="Surname:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Margin="70,0,0,5"
                 Width="265" HorizontalAlignment="Right"
                 Text="{Binding Employee.Surname, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="2" Content="Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Margin="70,0,0,5"
                 Width="265" HorizontalAlignment="Right"
                 Text="{Binding Employee.Name, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="3" Content="Patronymic:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Margin="70,0,0,5"
                 Width="265" HorizontalAlignment="Right"
                 Text="{Binding Employee.Patronymic, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="4" Content="Speciality:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Margin="70,0,0,5"
                 Width="265" HorizontalAlignment="Right"
                 Text="{Binding Employee.Speciality, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="5,10,0,0">
            <Button Content="Save"
                    Command="{Binding SaveCommand}"/>
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>